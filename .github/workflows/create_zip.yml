name: Build Zip File

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'browser-zipped-extesions/*'  # Replace with the actual path if needed 
    branches-ignore:
      - main  # Ignoring main here to avoid infinite loop


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chrome, firefox]

    steps:
    - name: Checkout main branch
      uses: actions/checkout@v2
      with:
        path: 'workspace'
        token: ${{ secrets.PA_TOKEN }}
        
        

    - name: Checkout ${{ matrix.browser }} branch for manifest.json
      uses: actions/checkout@v2
      with:
        ref: ${{ matrix.browser }}
        path: ${{ matrix.browser }}
        token: ${{ secrets.PA_TOKEN }}

    - name: Prepare extension directory
      run: |
        mkdir extension
        cp -r workspace/icons workspace/scripts extension/
        cp ${{ matrix.browser }}/manifest.json extension/

    - name: Zip extension
      run: |
        zip -r ${{ matrix.browser }}.zip extension

    - name: Move zip file to a separate directory
      run: |
        mkdir -p workspace/browser-zipped-extesions
        mv ${{ matrix.browser }}.zip workspace/browser-zipped-extesions/

    - name: Commit and Push
      uses: EndBug/add-and-commit@v7
      with:
        author_name: github-actions[bot]
        author_email: 41898282+github-actions[bot]@users.noreply.github.com
        message: 'Build ${{ matrix.browser }} extension'
        add: 'browser-zipped-extesions/*'
        default_author: github_actions
        cwd: 'workspace'
      if: github.ref == 'refs/heads/main'
